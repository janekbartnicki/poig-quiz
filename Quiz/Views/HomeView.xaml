<UserControl x:Class="Quiz.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quiz.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,10,0,0">
            <TextBlock Text="QUIZ"
                       FontSize="48"
                       FontWeight="Bold"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <StackPanel Orientation="Vertical" Width="200" HorizontalAlignment="Center" VerticalAlignment="Top" >
                <Button Command="{Binding NavigateQuizCommand}" Content="Wybierz Quiz" Margin="0,5" Padding="10"/>
                <Button Command="{Binding NavigateQuizGeneratorCommand}" Content="Stwórz Quiz" Margin="0,5" Padding="10"/>
                <Button Command="{Binding NavigateAuthorsCommand}" Content="Autorzy" Margin="0,5" Padding="10"/>
                <Button Command="{Binding ExitCommand}" Content="Wyjdź" Margin="0,5" Padding="10"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>

<Window x:Class="Quiz.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Quiz"
        xmlns:vm="clr-namespace:Quiz.ViewModel"
        xmlns:vw="clr-namespace:Quiz.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:QuizGeneratorViewModel}">
            <vw:QuizGeneratorView />
        </DataTemplate>
        
    </Window.Resources>
    <Grid>
        <TextBlock Text="Witaj w quizie!" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"/>
        <Button Content="Dodaj pytanie" Width="120" Height="30" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,240,0,0"/>
    </Grid>
</Window>

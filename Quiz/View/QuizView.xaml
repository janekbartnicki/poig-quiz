<UserControl x:Class="QuizApp.Views.QuizView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QuizApp.Views"
             xmlns:converters="clr-namespace:Quiz.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <Style TargetType="Button" x:Key="AnswerButtonStyle">
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Background" Value="#3498db"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Quiz Information -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="{Binding CurrentQuestionNumber, StringFormat='Pytanie {0}/{1}', ConverterParameter={Binding TotalQuestions}}" 
                       FontSize="18" Foreground="#7f8c8d"/>
            <ProgressBar Value="{Binding CurrentQuestionNumber}" Maximum="{Binding TotalQuestions}" 
                         Height="10" Margin="0,10,0,0" Background="#ecf0f1" Foreground="#2ecc71"/>
        </StackPanel>

        <!-- Question -->
        <TextBlock Grid.Row="1" Text="{Binding CurrentQuestion.QuestionText}" 
                   FontSize="22" TextWrapping="Wrap" Margin="0,0,0,20"/>

        <!-- Answers -->
        <ItemsControl Grid.Row="2" ItemsSource="{Binding CurrentQuestion.Answers}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding Text}" 
                            Command="{Binding DataContext.AnswerCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" 
                            CommandParameter="{Binding}"
                            Style="{StaticResource AnswerButtonStyle}"
                            IsEnabled="{Binding DataContext.CanAnswer, RelativeSource={RelativeSource AncestorType=ItemsControl}}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Navigation -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Feedback Panel -->
            <Border Grid.Column="0" 
                    Background="#f1c40f" 
                    Padding="10" 
                    CornerRadius="5"
                    Visibility="{Binding ShowFeedback, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="{Binding FeedbackMessage}" 
                               FontSize="16" 
                               Foreground="#2c3e50" 
                               TextWrapping="Wrap"/>
                </StackPanel>
            </Border>

            <!-- Next Button -->
            <Button Grid.Column="1" 
                    Content="Następne pytanie" 
                    Command="{Binding NextQuestionCommand}" 
                    Width="150" 
                    Height="40"
                    Background="#2ecc71"
                    Foreground="White"
                    FontSize="14"
                    BorderThickness="0"
                    Visibility="{Binding CanMoveNext, Converter={StaticResource BoolToVisibilityConverter}}">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Grid>
</UserControl>